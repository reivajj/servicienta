{
  "name": "@servicienta/types",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'types lint pending'",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "format": "echo 'types format pending'"
  }
}
